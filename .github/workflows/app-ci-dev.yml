name: App CI - Dev - Build & Push Images

on:
  workflow_run:
    workflows:
      - Terraform Apply - Dev
    types: [completed]

permissions:
  id-token: write
  contents: read

jobs:
   build-and-push-dev:
    name: Build & Push Images (dev)
    runs-on: ubuntu-latest
    environment: dev
    if: github.event.workflow_run.conclusion == 'success'

    env:
      IMAGE_TAG: sha-${{ github.event.workflow_run.head_sha }}
      PROJECT_ID: ${{ vars.GCP_PROJECT_ID }}
      REGISTRY: ${{ vars.DEV_REGISTRY }}
      CLUSTER_NAME: dev-cluster
      CLUSTER_ZONE: us-central1-a
      WIF_PROVIDER_DEV: ${{ secrets.WIF_PROVIDER_DEV }}
      DEV_SA: ${{ secrets.DEV_SA }}


    steps:
      # --------------------------------------------------
      # Checkout repository
      # --------------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      # --------------------------------------------------
      # Setup Docker Buildx
      # --------------------------------------------------
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # --------------------------------------------------
      # Validate required WIF secrets
      # --------------------------------------------------
      - name: Validate WIF secrets
        run: |
          if [ -z "${{ env.WIF_PROVIDER_DEV }}" ]; then
            echo "❌ Secret WIF_PROVIDER_DEV is not set or empty."
            echo "Add the full WIF provider resource name to the 'WIF_PROVIDER_DEV' secret."
            exit 1
          fi
          if [ -z "${{ env.DEV_SA }}" ]; then
            echo "❌ Secret DEV_SA is not set or empty."
            echo "Add the service account email to the 'DEV_SA' secret."
            exit 1
          fi

      # --------------------------------------------------
      # Authenticate to GCP (WIF)
      # --------------------------------------------------
      - name: Authenticate to GCP (WIF)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WIF_PROVIDER_DEV }}
          service_account: ${{ env.DEV_SA }}
          project_id: ${{ env.PROJECT_ID }}
          create_credentials_file: true
          export_environment_variables: true
          universe: googleapis.com
          cleanup_credentials: true
          access_token_lifetime: 3600s
          access_token_scopes: https://www.googleapis.com/auth/cloud-platform
          id_token_include_email: false

      # --------------------------------------------------
      # Setup gcloud CLI
      # --------------------------------------------------
      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2

      # --------------------------------------------------
      # Check DEV GKE cluster
      # --------------------------------------------------
      - name: Check if DEV GKE cluster exists
        id: check_cluster
        run: |
          if gcloud container clusters describe "$CLUSTER_NAME" \
            --zone "$CLUSTER_ZONE" \
            --project "$PROJECT_ID" >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Stop CI if cluster does not exist
        if: steps.check_cluster.outputs.exists == 'false'
        run: |
          echo "⚠️ Dev cluster does not exist. Skipping CI."
          exit 0

      # --------------------------------------------------
      # Docker authentication
      # --------------------------------------------------
      - name: Configure Docker Auth
        run: gcloud auth configure-docker $REGISTRY --quiet

      # --------------------------------------------------
      # Run unit tests
      # --------------------------------------------------
      - name: Run unit tests
        run: echo "✅ Running unit tests (dev)"

      # --------------------------------------------------
      # Build & push images
      # --------------------------------------------------
      - name: Build & Push Images
        run: |
          SERVICES=(application-service patient-service order-service)
          for S in "${SERVICES[@]}"; do
            echo "🔨 Building & pushing $S..."
            docker buildx build \
              --platform linux/amd64 \
              --pull \
              --push \
              -t $REGISTRY/$S:${IMAGE_TAG} \
              ./services/$S
          done
