apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "patient-service.fullname" . }}
  labels:
    app: {{ include "patient-service.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "patient-service.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "patient-service.name" . }}
    spec:
      {{- if .Values.global.serviceAccount.enabled }}
      serviceAccountName: {{ .Values.global.serviceAccount.name | quote }}
      {{- end }}

      containers:
        # --------------------------------
        # Patient Service (Node.js)
        # --------------------------------
        - name: {{ include "patient-service.name" . }}
          image: "{{ required "ERROR: image.repository is required for patient-service" .Values.image.repository }}:{{ required "ERROR: image.tag is required for patient-service" .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}

          env:
            {{- range .Values.env }}
            - name: {{ .name }}
              {{- if .value }}
              value: {{ .value | quote }}
              {{- else if .valueFrom }}
              valueFrom:
{{ toYaml .valueFrom | nindent 16 }}
              {{- end }}
            {{- end }}

          resources:
{{- toYaml .Values.resources | nindent 12 }}

        # --------------------------------
        # Cloud SQL Auth Proxy (Sidecar)
        # --------------------------------
        {{- if .Values.global.cloudSql.enabled }}
        - name: cloud-sql-proxy
          image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.11.0
          args:
            - "--port=5432"
            - "{{ .Values.global.cloudSql.instance }}"
          securityContext:
            runAsNonRoot: true
        {{- end }}
